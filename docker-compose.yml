services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: tiffinflow
      POSTGRES_USER: tiffinflow
      POSTGRES_PASSWORD: tiffinflow
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
